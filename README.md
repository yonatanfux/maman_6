# Maman_6

GROUP_SEED: 413134

How to run:

## Usage: 
from main folder:

```python
python -m crack [-h] 
--defense {no-defense, totp, captcha, rate-limit, account_lock}
 --hash-mode {SHA_PLAIN, SHA_SALT, SHA_PEPPER, SHA_SALT_PEPPER, BCRYPT, BCRYPT_PEPPER, ARGON2, ARGON2_PEPPER}
```